# WinTabsGo

**语言：** **中文** | [English](version_English.md)

版本 zh_2026.09.26.8。维护者 zhihuikeji。

本文件只记录 zh_2026.09.26.8 在上游 WindowTabs（至 ss_2026.09.21，原作者 Maurice Flanagan，ss_ 线维护者 Satoshi Yamamoto）之上新增和修复的内容，不含上游逐版历史。

## version zh_2026.09.26.8

- 任务管理器可以挂上标签。原先固定不给 taskmgr.exe 加标签，现已取消。

## version zh_2026.09.26.7

- 首次启动可以选择管理员权限。点「允许」后，Windows 询问一次授权。授权成功后，以后启动会自动使用管理员权限。点「以后再说」则继续以普通权限运行，标签功能受限。
- 授权窗口说明高权限窗口会遮挡标签栏，并提示授权失败时先结束 WinTabsGo 进程，再重新启动。

## version zh_2026.09.26.6

- 标签栏右键菜单底部、托盘图标右键「关闭 WinTabsGo」下方增加「关于WinTabsGo」。英文界面显示 About WinTabsGo。
- 关于窗口跟随设置里的黑白主题。窗口加大，正文按标题、版本信息和功能分节排版，四周留白。
- 关于窗口只保留一条细滚动条，底色与正文相同，不再出现白色底框。

## version zh_2026.09.26.3


- 鼠标停在屏幕占比上弹出菜单后，若没有移进菜单就直接移走，菜单会关掉。移到其他标签会按悬停继续切换，不用再点一下。

## version zh_2026.09.26.2

- 屏幕占比菜单增加全屏。未全屏时显示「全屏」，点下去走窗口右上角的最大化。已经全屏时显示「退出全屏」，点下去走右上角的还原。组里还有远程桌面独占时，退出会被拦住，并弹出现有警告。英文界面显示 Full screen 和 Exit full screen。

## version zh_2026.09.26.1


- 普通页进入全屏后，同一组里的远程桌面只把外框铺到同一块区域，仍是普通窗口，不进入独占。
- 切到某个远程桌面，再点它自己的右上角，才让这一个进入独占。组里其他远程桌面不会跟着进入，需要各自切过去再点一次。
- 组里还有远程桌面处于独占时，其他页退出全屏或拖动标题栏不会让整组退出，并提示：本组有远程桌面独占，请检查后关闭再点击退出全屏。在该远程桌面页退出独占之后，其他页再退出，整组才退出全屏。提示按界面语言显示中文或英文，播放系统警告音，出现在当前屏幕中央。第一行标题比正文大，文字居中。留着不关则六秒后自动关闭；用标题栏关闭按钮或确定关掉后，再拖动会立刻再弹出。
- 组进入全屏后，同组其他窗口跟着铺满。远程桌面只铺满，不进入独占；只有点到该远程桌面自己的右上角，它才进入独占。进入独占时，其余窗口同样跟着全屏。
- 退出远程桌面独占后，不再改整组其他窗口的大小。
- 托盘语言菜单只保留两项。界面为中文时显示「语言 / 英文 / 中文」，界面为英文时显示「Language / English / Chinese」。
- 每个标签组的标签条末尾多一格屏幕占比，外形与标签相同，并跟着标签停在左侧或右侧。点一下或掠过，可选 25%、50%、75%。没有独占时，窗口以当前屏幕中心为中心，宽高各占该百分比。组里还有独占时不改大小，只弹出原来的警告。在同一块屏幕上拖动标题栏或拉边框会取消这个比例；拖到另一块屏幕则按新屏幕重新居中。

## version zh-2026092501

### 产品

- 设置目录是 `%APPDATA%\WinTabsGo`。不读取、不复制旧的 `%APPDATA%\WindowTabs`。
- 已去掉联网检查更新，可在内网或断网环境使用。

### 在原作者功能上新增

- **标签可停靠四边。** 设置 → 行为中可选居上、居下、居左、居右。居上与上游行为一致。居下是窗口内部的底边横条。居左、居右是窗口内部的竖条，点击位置与旋转后的标签对齐。默认仍是居上。尚未做按标签组分别选择停靠边。
- **标签上的关闭按钮默认关闭。** 行为页可以再打开。行为页里，停靠边和其下方选项排在同一行。
- **鼠标悬停激活标签。** 可设置悬停多久后切换，默认 200 毫秒。
- **远程桌面全屏。** 全屏时保留标签条；可选择全屏时不隐藏标签。退出全屏后，同一组里的窗口位置会跟着恢复，避免把远程桌面连接栏挤乱。
- **设置改为手动保存。** 对话框里的修改先记在草稿里，点保存后才写入磁盘并作用到窗口和快捷键。关闭未保存的设置时，会询问保存、不保存或取消。
- **设置界面说明。** 外观、行为、快捷键和工作区增加说明行；数值项更紧凑；设置窗口加宽。快捷键页的数字键与热键框对齐，Ctrl/Alt 模式在同一行。

### 在原作者功能上修复

- 保存设置后会正确应用外观和自定义标签颜色。减轻保存时界面卡住，并修复打开设置时崩溃、深色模式切换后缓存不对、快捷键数值读不到某些 JSON 类型。
- 四个方向的标签提示按所在边定位，并限制在窗口客户区内。
- 全屏时拖动标题栏不再闪回满屏。拖动结束后再同步同组窗口的位置。
